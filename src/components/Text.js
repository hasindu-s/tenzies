import React from "react";

export default function Text() {
    return (
        <div className="text">
            <h1 className="text-title">Tenzies</h1>    
            <h3 className="text-description">Roll until all dice are the same. Click each die to freeze it at its current value between rolls.</h3>
        </div>
    );
}